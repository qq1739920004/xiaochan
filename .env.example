# Web access port on this Mac
APP_PORT=8080

# Database values used only by the Docker stack
MYSQL_DB=xiaocan
MYSQL_ROOT_PASSWORD=root123456

# Paste the existing Cloudflare Tunnel token here. Do not commit the real value.
CLOUDFLARE_TUNNEL_TOKEN=
