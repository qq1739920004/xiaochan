# 大牌券连续窗口实施计划

> 执行方式：在当前会话按任务逐项实施并验证。

**目标：** 在每日 `09:29:57.000-09:30:01.000` 期间按 `100-300` 毫秒随机间隔连续请求大牌券接口。

**架构：** 定时服务根据默认 cron 创建连续窗口任务；执行器负责精确等待、窗口循环、终态停止和首请求时间记录；前端只展示固定窗口和固定间隔，避免保存为旧的 5 次策略。

**技术栈：** Spring Boot、Java 17、Vue 3、Element Plus、JUnit 5、Docker Compose。

## 全局约束

- 时区固定为 `Asia/Shanghai`。
- 默认开始时间为 `09:29:57.000`，结束时间为 `09:30:01.000`。
- 请求间隔固定为 `100-300` 毫秒随机值。
- `40026` 和网络临时错误继续；成功、已抢完、已领取、登录失效、需要验证立即停止。
- 日志不得输出登录态或其他凭证。

### 任务 1：连续窗口执行器

**文件：**
- 修改：`src/main/java/io/github/xiaocan/service/BrandCardClaimExecutor.java`
- 修改：`src/test/java/io/github/xiaocan/service/BrandCardClaimExecutorTest.java`

- [ ] 先新增失败测试，验证执行器在 `09:29:57.000` 才发送首请求，收到 `40026` 后继续，超过 `09:30:01.000` 停止。
- [ ] 运行指定测试并确认因缺少连续窗口方法失败。
- [ ] 新增连续窗口方法，使用传入的开始和结束时间、100-300 毫秒间隔以及终态停止判断。
- [ ] 重新运行指定测试并确认通过。

### 任务 2：定时配置与页面

**文件：**
- 修改：`src/main/java/io/github/xiaocan/service/impl/BrandCardClaimServiceImpl.java`
- 修改：`frontend/src/views/BrandCardClaimView.vue`

- [ ] 设置默认 cron 为 `57 29 9 * * ?`，目标时间不再附加秒数。
- [ ] 使用连续窗口执行器，结束时间固定为目标时间后 4 秒，最多次数设为高于窗口理论次数的保护值。
- [ ] 页面展示 `09:29:57-09:30:01` 和 `100-300ms`，禁用旧的次数与间隔配置。
- [ ] 保存配置时固定写入窗口参数。

### 任务 3：验证与发布

**文件：**
- 修改：同上

- [ ] 运行大牌券执行器和请求构造单元测试。
- [ ] 运行后端打包、前端生产构建与 Compose 配置校验。
- [ ] 使用中文提交信息提交代码，并同步到 `feature/auto-grab` 和 GitHub。
